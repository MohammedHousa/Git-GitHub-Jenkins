# Connect to a remore repository
git remote add origin <repository link>

# Check if your connected to any remote repository
git remote -v

# Push your commited code to GitHub repositoty
git push origin master

# Check your branches
git branch

# Create a new branch
git branch <new branch>

# Go to a different branch
git checkout <branch name>

# Push your commmited code to your branch
git push -u <branch name>

# Merge the master with your branch
git merge <new branch>

# Grap the logs from the main branch
git rebase master
